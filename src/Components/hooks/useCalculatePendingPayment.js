// import { findByContractId } from "../../Modules/documents/utils/findByContractId";

import { useState } from "react";

const useCalculatePendingPayment = (total) => {

    const handlePendingPayment = () => {

    }
};

export default useCalculatePendingPayment;